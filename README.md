# IP Address Finder

This simple flask application return the IP ADDRESS of the user visiting the webpage.

# Features
- Display the visitor's IP address
- Works behind proxies using 'X-Forwarded-For'
- Containerized the flask app using Docker tool
- Deployable on any server like EC2 Instance

# This project contains
- app.py
- requirements.txt
- Dockerfile
